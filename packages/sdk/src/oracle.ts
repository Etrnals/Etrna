export class EtrnaOracleClient {
  async requestAI(_uefId: number, _prompt: string, _requesterAddress: string) {
    // Placeholder interaction with AIRestakedOracle contract.
    return { chainRequestId: 0, txHash: '0x' };
  }
}
