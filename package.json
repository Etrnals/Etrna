{
  "name": "etrna",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@8.15.8",
  "scripts": {
    "compile": "hardhat compile",
    "test": "pnpm run contract:test",
    "contract:test": "hardhat test",
    "build": "pnpm run compile && pnpm run frontend:build",
    "frontend:build": "pnpm -C frontend build",
    "deploy": "node scripts/deploy.js",
    "start": "node backend/server.js",
    "lint": "echo 'No lint task configured'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.20.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "cors": "^2.8.5",
    "ethers": "^6.7.1",
    "express": "^4.19.2",
    "keccak256": "^1.0.6",
    "merkletreejs": "^0.6.0"
  }
}
